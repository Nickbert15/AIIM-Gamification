# LHG AI Enablement Platform

A gamified learning platform for Finance/Controlling staff to practice working with AI. Players
complete AI challenges (Excel co-piloting, prompt writing, hallucination spotting), earn points on
a live leaderboard, and rate each game. Admins generate new games through an LLM pipeline (n8n +
KIconnect), review/regenerate/approve them before they go live, and track usage and feedback.

---

## Requirements

- Node.js 18+
- A Supabase project (Postgres)
- A KIconnect API key (LLM gateway)
- (optional) An n8n instance with the four game-generation webhooks — only needed to generate new games

## Setup

```bash
npm install
cp .env.local.example .env.local   # then fill in your values, see below
npm run dev                        # → http://localhost:3000
```

| Setting | Notes |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase Dashboard → Settings → API |
| `SUPABASE_SERVICE_ROLE_KEY` | Same page. Server-only — never expose to the client. |
| `JWT_SECRET` | Signs session tokens. Falls back to the service-role key if unset. |
| `KICONNECT_API_URL` / `KICONNECT_API_KEY` | LLM gateway used for in-game AI calls and game-content regeneration. |
| `N8N_EXCEL_WEBHOOK_URL`, `N8N_HALLUCINATION_WEBHOOK_URL`, `N8N_ARENA_WEBHOOK_URL`, `N8N_BRANCHING_WEBHOOK_URL` | One generation webhook per game type. Only needed to generate *new* games. |

## Database

Two SQL files need to be run once in the Supabase SQL Editor — there's no migration pipeline yet:

```
supabase/game_feedback.sql                             # player feedback (1-3 stars + comment)
supabase/migrations/20260826_add_ai_process_logs.sql   # audit log for every AI/LLM call
```

Everything else (`players`, `scores`, `games`, `technologies`) is managed directly in the Supabase
project — see [docs/ARCHITEKTUR.md](docs/ARCHITEKTUR.md) for the full schema.

## Deploy

```bash
npx vercel --prod
# then set the same env vars in the Vercel dashboard (Settings → Environment Variables)
```

## Pages

| Route | Description |
|---|---|
| `/` | Public leaderboard (realtime) |
| `/player-dashboard` | Player home: stats, recommendations, games to play |
| `/admin` | Admin overview (key metrics) |
| `/admin/players` | Manage players |
| `/admin/scores` | View & delete scores |
| `/admin/games` | Generate, review, regenerate, and approve games |
| `/admin/feedback` | Feedback breakdown per game type |

## n8n integration

New games are generated by posting to the `N8N_*_WEBHOOK_URL` for the chosen game type (see
[`/api/generate`](src/app/api/generate/route.ts)); the workflow writes the finished game straight
to Supabase with `status = draft`.

n8n can also write scores directly:

```
POST https://YOUR_PROJECT.supabase.co/rest/v1/scores
Headers:
  apikey: <service_role_key>
  Authorization: Bearer <service_role_key>
  Content-Type: application/json
Body:
  { "player_id": "uuid", "game_id": "quiz_001", "score": 850 }
```

## Tests

```bash
npm test
```

## More detail

Full architecture, data model, auth flow, and the AI-process audit log are documented in
[docs/ARCHITEKTUR.md](docs/ARCHITEKTUR.md).
